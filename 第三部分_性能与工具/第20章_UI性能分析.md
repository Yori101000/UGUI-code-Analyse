# 第20章 UI 性能分析

> 本文是对原文第 19 章的结构化重写。原文 6 节约 2 万字但核心结论高度重复——"材质变化打断 Batch"在 19.1、19.2、19.3 各讲了一遍，"Outline 顶点×5"的案例在 19.4.2、19.4.7、19.5.4 重复出现，"Mask 不减少 Overdraw"在 19.3.6 和 19.6.6 重复。现合并为 5 个性能维度，每个维度列出核心问题、原因链、诊断工具和优化方向。

---

## 概述

前三章分别讲了怎么构建顶点（第 17 章）、怎么通过 Shader 渲染（第 18 章）、怎么实现特效（第 19 章）。本章反过来——从性能角度审视这些操作到底有什么代价。

```
UGUI 性能问题不是单一模块导致的，而是整个渲染体系共同叠加的结果。
一个 Outline 组件可能让 1 个 DrawCall 变成 3 个，
一个 Mask 嵌套可能让 GPU FillRate 翻倍，
一个 graphic.material 赋值可能让共享材质全部实例化。
```

UI 性能问题可以归纳为五个维度：

| # | 问题 | 影响谁 | 诊断工具 |
|---|------|--------|---------|
| 1 | **Batch 中断** | CPU（DrawCall） | Frame Debugger → Batch Breaking Reason |
| 2 | **材质实例化** | CPU（DrawCall）+ 内存 | Frame Debugger → Different Material |
| 3 | **Mask/Stencil 状态** | CPU（材质实例）+ GPU（Fragment） | Frame Debugger → Different Stencil State |
| 4 | **顶点膨胀** | CPU（Canvas Rebuild）+ GPU（Vertex） | Profiler → Canvas.BuildBatch, Stats → Verts |
| 5 | **Overdraw** | GPU（FillRate） | Scene View → Overdraw Draw Mode |

---

## 20.1 Batch 中断

### 合批条件：精确匹配

UGUI 的 Batch 合并条件是**精确匹配**，不是"相似即可"：

```
两个 UI 元素必须拥有完全一致的：
  ├── Material（同一个 C# 对象实例）
  ├── Texture（同一张纹理）
  ├── Shader（同个 Shader + 同组 Keyword）
  ├── Stencil 状态（Ref / Comp / Pass 相同）
  ├── RectMask2D 裁剪矩形（相同或都未设置）
  ├── Canvas（属于同一个 Canvas）
  └── Additional Shader Channels（一致）
```

只要其中任意一项不同 → Batch 中断 → 新 DrawCall。

### 渲染顺序的影响

UGUI 是**严格层级渲染系统**，渲染顺序由 Hierarchy 中的节点顺序决定，不会为了合批打乱顺序：

```
Hierarchy：A（材质 M1）→ B（材质 M2）→ C（材质 M1）
结果：Batch 0 → A | Batch 1 → B | Batch 2 → C
```

即使 A 和 C 材质相同，它们也无法合并。因为 Unity 为了保证 UI 层级正确，必须先画 A、再画 B、最后画 C。

**实操建议**：把相同材质的 UI 元素在 Hierarchy 中排在一起——可以减少中间被不同材质打断的次数。

### Canvas 是 Batch 的天然边界

不同 Canvas 之间的 UI 即使材质、纹理完全相同，也分别提交独立 DrawCall。

但同一个 Canvas 太大会导致另一个问题：

```
超大 Canvas：任意子节点变化 → 整个 Canvas 重建 → 所有 Mesh 重新生成
多个小 Canvas：DrawCall 增加（每个 Canvas 独立提交）
```

**解决方案**：按更新频率拆分 Canvas：

| Canvas | 内容 | 更新频率 |
|--------|------|---------|
| 静态 Canvas | 背景、装饰、Logo | 几乎不变 |
| 动态 Canvas | 血条、数字、倒计时 | 中等频率 |
| 高频 Canvas | 聊天、实时数值 | 频繁更新 |

这样即使高频 Canvas 不断重建，也不会影响静态 Canvas 的 Mesh。

---

## 20.2 材质实例化

### 先纠正一个流传很广的误解

网上常见的说法是「`graphic.material` 的 getter 会自动 `Instantiate()` 出新实例，`sharedMaterial` 不会」。**这是 `Renderer`（MeshRenderer 等）的语义，UGUI 的 `Graphic` 不是这样的，也根本没有 `sharedMaterial` 属性。**

对照 main 的 `Graphic.cs`：

```csharp
// Graphic.cs（main）
public virtual Material material
{
    get { return (m_Material != null) ? m_Material : defaultMaterial; }
    set
    {
        if (m_Material == value)
            return;
        m_Material = value;
        SetMaterialDirty();      // ← 触发重建的是 setter，不是 getter
    }
}
```

三个结论：

- **getter 没有任何副作用**：只返回你设过的材质，或 `defaultMaterial`（UGUI 内置共享材质）。读它不实例化、不打 Dirty、不断批。
- **`Graphic` 上不存在 `sharedMaterial`**：`image.sharedMaterial` 编译不过（`Graphic` 继承自 `UIBehaviour`，不是 `Renderer`）。
- **真正触发重建的是赋值**：`graphic.material = xxx` 会调 `SetMaterialDirty()`，下一帧走 `UpdateMaterial()`。

### 材质实例到底从哪来

UGUI 里多出来的 Material 实例只有三个来源：

| 来源 | 说明 | 是否可控 |
|------|------|:---:|
| **你自己创建并赋值** | `graphic.material = new Material(shader)` / `Instantiate(mat)` | ✅ 完全可控 |
| **Mask 的 Stencil 材质** | `StencilMaterial.Add()` 按 Stencil 参数生成材质副本（**有缓存**，相同参数复用同一实例，见第 15 章） | ⚠️ 由 Mask 层级决定 |
| **第三方特效插件** | 内部 `new Material(baseMaterial)` | ❌ 取决于插件实现 |

注意 `Graphic.materialForRendering` **不在这个名单里**：它是只读 getter，作用是把 `material` 依次交给同 GameObject 上的所有 `IMaterialModifier` 处理（Mask 的 Stencil 注入就发生在这里），**访问它本身不创建实例**。

```csharp
// Graphic.cs（main，materialForRendering 的逻辑）
public virtual Material materialForRendering
{
    get
    {
        var components = ListPool<IMaterialModifier>.Get();
        GetComponents<IMaterialModifier>(components);
        var currentMat = material;
        for (var i = 0; i < components.Count; i++)
            currentMat = components[i].GetModifiedMaterial(currentMat);
        ListPool<IMaterialModifier>.Release(components);
        return currentMat;
    }
}
```

### 真正的性能陷阱

不是"读 material"，而是下面这两个：

```csharp
// ❌ 陷阱一：每个 UI 各给一份材质
// 100 个 Image 各自 new 一份 → 100 个材质实例 → 100 次断批
foreach (var img in images)
    img.material = new Material(uiShader);

// ✅ 共享同一个实例
foreach (var img in images)
    img.material = sharedUIMat;   // 同一引用 → 可以合批
```

```csharp
// ❌ 陷阱二：直接改共享材质的参数，影响面失控
// Graphic.material 返回的可能就是那个被所有 UI 共用的实例
image.material.SetFloat("_Gray", 1f);   // 所有引用该材质的 UI 一起变灰

// ✅ 要单独控制某个 UI 的效果，显式做一份实例并自己管理生命周期
if (myMat == null) myMat = new Material(image.material);
image.material = myMat;
myMat.SetFloat("_Gray", 1f);
// 注意：这一步就制造了一次断批，是有意为之的取舍
```

**一句话**：UGUI 不会背着你实例化材质，但也不会背着你保护共享材质。多出来的 DrawCall 都来自"你赋了一份新材质"，而不是"你读了 material"。

### 哪些 UI 特效会隐式创建材质实例

| 场景 | 原因 | 诊断标志 |
|------|------|---------|
| Mask 组件 | `StencilMaterial.Add()` 生成带 Stencil 参数的材质副本（同参数有缓存，不会每个子元素一份） | Frame Debugger → Different Stencil |
| 第三方特效插件 | 内部 `new Material(baseMaterial)` | 不同 Material 实例 |
| Outline / Shadow | 内置组件**不创建新材质**（通过 ModifyMesh 改顶点实现） | 不涉及材质变化 |
| 给 `graphic.material` 赋值 | 赋一个新建的 Material → 该 Graphic 独占一份，与他人断批 | Material 数量增加 |
| 运行时改 Shader 参数 | `graphic.material.SetFloat()` **不会**实例化——它直接改到共享材质上，影响所有引用者 | 一改全变，而非 Material 数量增加 |

### Shader Keyword 导致的隐式断批

同一个 Shader 文件，但不同的 Keyword 组合，在 GPU 视角中是不同的 Shader Variant：

```
Image A → UNITY_UI_CLIP_RECT ON
Image B → UNITY_UI_CLIP_RECT OFF
```

即使它们引用同一个 `ui/default` Shader 文件，Batch 也会被打断——GPU 加载的已经是两个不同的着色器程序。

---

## 20.3 Mask/Stencil 的代价

> 这一节和第 15 章（Mask 裁剪机制）内容重叠。本章只从性能维度补充关键数据。

### Mask 增加 DrawCall 的原因链

```
Mask 组件挂上
  → StencilMaterial.Add() 创建新材质实例（带 Stencil 参数）
    → Mask 自身 Image 使用这个材质
      → 子节点使用另一个 Stencil 材质（不同 Stencil 参数）
        → 外部的 UI 使用普通材质（没有 Stencil）
          → 三个不同材质实例 → 至少 3 个 Batch
```

每层嵌套 Mask 增加一组新的 Stencil 参数 → 每个参数组合对应一个新的材质实例 → Batch 按 Stencil 深度分裂。

### Mask 不减少 Overdraw

**不要指望用 Mask 或 RectMask2D 来省 Overdraw。** 但两者的原因不同：

| | 裁剪发生在哪 | 片元着色器是否执行 |
|---|---|---|
| **RectMask2D** | 片元着色器**内部**（`color.a *= UnityGet2DClipping(...)`） | ✅ 一定完整执行——被裁掉的像素是"算完后 alpha 变 0" |
| **Mask（Stencil）** | 逐片元的 Stencil 测试 | ⚠️ 取决于硬件：现代 GPU 支持 Early Stencil Test（着色器**之前**拒绝），但 Stencil 配了写入操作（`Replace`）或着色器里有 `clip()/discard` 时可能失效 |

> 上表为引擎与 GPU 行为，C# 侧无源码，属**行为推断**。详细讨论见第 18 章 18.4.3、第 15 章 15.3.3。

结论：裁剪系统解决的是"哪些像素可见"，不是"少跑多少着色器"。真要降 FillRate，得减少 UI 元素的实际重叠面积。

### RectMask2D vs Mask

RectMask2D 不依赖 Stencil，通过 `CanvasRenderer.EnableRectClipping()` 设置裁剪矩形，GPU 侧由 UI Shader 的 `UNITY_UI_CLIP_RECT` + `_ClipRect` 在片元阶段裁剪，**不产生材质实例化**。

但注意「不实例化材质」不等于「不影响 Batch」：裁剪矩形本身就是渲染状态，**同一个 RectMask2D 下的元素可以合批，分属不同 RectMask2D 的元素之间仍会断批**（与 20.1 合批条件表中的「RectMask2D 裁剪矩形」一致）。

即便如此，凡是矩形裁剪场景仍应优先用 RectMask2D —— 它的断批粒度是"每个裁剪区一次"，而 Mask 是"每个 Stencil 深度一次 + 材质实例"，后者通常差得多。完整对比见第 15 章。

---

## 20.4 顶点膨胀

UGUI 是 Mesh 渲染系统。顶点数量直接影响：

| 阶段 | 影响 |
|------|------|
| CPU | `OnPopulateMesh`、`ModifyMesh`、`VertexHelper` 操作 |
| CPU | `Canvas.BuildBatch()` 处理大量顶点 |
| CPU→GPU | 大量顶点数据上传 |
| GPU | Vertex Shader 处理 |

### 顶点膨胀的乘法效应

```
Text（1000 字符 = 4000 顶点）
  + Outline（×5）= 20000 顶点
    + Shadow（×2）= 40000 顶点
```

Outline 的 ×5 是对**已有全部顶点**做 4 方向复制（左右上下），不是对原始顶点。

### 过量顶点的诊断标准

| 顶点数 | 风险 | 典型场景 |
|--------|------|---------|
| < 2K | ✅ 安全 | 简单 UI |
| 2K ~ 10K | ⚠️ 注意 | 少量 Text |
| 10K ~ 50K | ❌ 严重 | 大量 Text + Outline |
| 50K+ | 🔴 灾难 | 长列表 + 多层特效 |

在 Stats 面板的 `Verts` 窗口看到超过 10K 就要排查了。

> ⚠️ 上表是以**中低端移动设备**为参照的经验阈值，不是硬性标准：桌面端可以显著放宽，高端机型也不会在 10K 顶点上出问题。真正的判断依据永远是目标设备上的 Profiler 采样（`Canvas.SendWillRenderCanvases` / `Graphic.Rebuild` 的实际耗时），而不是顶点数本身。顶点数只是"值得去看一眼"的信号。

---

## 20.5 Overdraw

### 为什么 UI 天然容易 Overdraw

3D 不透明物体可以利用深度测试提前剔除被遮挡像素（Early-Z），被遮挡的 Fragment Shader 根本不会执行。但 UI 使用透明混合（`Blend SrcAlpha OneMinusSrcAlpha`），无法使用 Early-Z——GPU 必须执行 Fragment Shader，然后才能做 Blend 决定最终颜色。

```
全屏半透明遮罩 → 每个像素一次 Fragment
  弹窗背景 → 第二次（弹窗区域）
    按钮  → 第三次
      文字 → 第四次
```

### 最危险的 Overdraw 来源

| 来源 | 危害程度 | 原因 |
|------|---------|------|
| 全屏半透明遮罩 | 🔴 极高 | 覆盖所有像素，每次整屏 Fragment |
| UI Blur | 🔴 极高 | GrabPass + 双 Pass 模糊，全屏 Fragment |
| 大面积发光/Glow | 🟡 高 | 81 次采样的代价 × Overdraw |
| 多层半透明叠加 | 🟡 高 | 弹窗套弹窗，每层都混合 |
| Text 叠 Outline | 🟡 中 | 透明区域大，字符边缘重复采样 |

### 诊断 Overdraw

Scene View → Draw Mode → Overdraw：
- 黑色 = 无 Overdraw
- 绿色 = 中度
- **红色 = 严重**
- **白色 = 爆炸（移动端必卡）**

---

## 优化策略优先级

```
第一优先级：减少 Canvas 重建
  按更新频率拆分 Canvas
  静态 UI 和动态 UI 分离
  避免每帧 SetVerticesDirty()

⚠️ 常见误区：只盯着 DrawCall，忽略 Rebuild 成本
  把所有 UI 合并到一个 Canvas 可以减少 DrawCall（不同 Canvas 无法合批），
  但代价是任意一个小变化都触发整个 Canvas 重建。
  DrawCall 降了，CPU 开销反而涨了。
  正确做法：在 DrawCall 和 Rebuild 之间做取舍——
  静态内容合并（省 DrawCall），动态内容拆分（省 Rebuild）。

第二优先级：减少 Batch 中断
  相同材质的 UI 在 Hierarchy 中排在一起
  统一图集、Shader、材质
  不用 graphic.material 写操作

第三优先级：控制顶点
  避免 Text + Outline + Shadow 组合
  ScrollView 中的节点复用
  少用富文本

第四优先级：降低 Overdraw
  减少全屏半透明遮罩
  Blur / Glow 限制在小范围
  移动端特别注意 Fragment 压力

第五优先级：Layout 系统
  LayoutGroup 适合编辑器排版，不适合高频动态更新
  长列表手动控制坐标
```

---

## 20.6 大规模项目工程优化

前 5 节讲的都是"单界面的渲染性能"。当项目有几十上百个界面时，还有一套工程层面的问题——界面切换卡顿、GC 频繁、列表滚动掉帧。这不在渲染管线的范畴，但影响更大。

### 20.6.1 UI 分层架构

大型项目通常按**界面层级类型**拆分 Canvas，而不是按"功能模块"拆：

```
UIRoot
├── BottomLayer Canvas  ← 底层主界面（常驻、很少刷新）
├── WindowLayer Canvas  ← 功能窗口（背包、商城）
├── PopupLayer Canvas   ← 弹窗层（确认框、奖励弹窗）
├── TopLayer Canvas     ← 顶层提示（Toast、飘字）
├── GuideLayer Canvas   ← 新手引导
└── LoadingLayer Canvas ← Loading 界面
```

每一层独立 Canvas，互不影响。这样底层主界面的静态 UI 不会因为弹窗打开而重建，弹窗也不会因为底层刷新而重建。**分层 = 隔离重建范围。**

拆分原则按刷新频率：

| Canvas 类型 | 内容 | 更新频率 | 典型位置 |
|-----------|------|---------|---------|
| 静态 Canvas | 背景、装饰、固定按钮 | 几乎不变 | BottomLayer |
| 动态 Canvas | 血量、金币、倒计时 | 中等频率 | 窗口层内独立 |
| 高频 Canvas | 聊天、实时排行榜 | 频繁 | 单独子 Canvas |
| ScrollView Canvas | 列表内容 | 持续变化 | 模块内独立 |
| 动画 Canvas | Tween 动画、渐变 | 每帧变化 | 独立 Canvas |

### 20.6.2 UI 生命周期管理

很多项目卡顿不是因为渲染，而是因为界面管理混乱——同一个界面重复打开、事件重复注册、关闭后资源不释放。

标准化的 UI 生命周期：

```
Create     → 实例化 Prefab，创建节点
Initialize → 绑定事件、注册监听器
Show       → 播放入场动画、刷新数据
Refresh    → 更新显示内容（数据变化时）
Hide       → 播放退场动画
Destroy    → 解绑事件、释放资源、回收入池
```

关键原则：
- **Create/Initialize 只执行一次**，Show/Hide 可以反复调用
- **Destroy 不一定是真销毁**——可以用缓存池替代
- **退出时一定要解绑事件**，否则界面对象永远无法 GC

### 20.6.3 对象池与 UI 缓存池

**UI 缓存池**（界面级别）：关闭的界面不 Destroy，放入缓存池，下次直接 Show 复用：

```
首次打开背包 → Instantiate
  关闭背包 → SetActive(false)，入池
    再次打开背包 → SetActive(true)，刷新数据，直接显示
```

避免了每次打开界面都走 Instantiate、加载资源、重建 Canvas 的完整链路。

**对象池**（列表节点级别）：适用于 ScrollView 中大量重复的 Item。

```csharp
public class UIObjectPool<T> where T : Component
{
    private Queue<T> pool = new Queue<T>();
    private T prefab;
    private Transform parent;

    public UIObjectPool(T prefab, int preloadCount, Transform parent)
    {
        this.prefab = prefab;
        this.parent = parent;
        for (int i = 0; i < preloadCount; i++)
        {
            T obj = GameObject.Instantiate(prefab, parent);
            obj.gameObject.SetActive(false);
            pool.Enqueue(obj);
        }
    }

    public T Get()
    {
        if (pool.Count > 0)
        {
            T obj = pool.Dequeue();
            obj.gameObject.SetActive(true);
            return obj;
        }
        return GameObject.Instantiate(prefab, parent);
    }

    public void Release(T obj)
    {
        obj.gameObject.SetActive(false);
        pool.Enqueue(obj);
    }
}
```

### 20.6.4 虚拟列表（Virtual Scroll）

传统 ScrollView 的问题：数据 1000 条 → 创建 1000 个 Item → 节点暴涨 → Canvas 重建爆炸 → 卡顿。

但屏幕一次只能看到 10~15 个 Item。虚拟列表的核心：**只创建可见区域的节点，滚动时循环复用。**

```
屏幕可见区：10 个 Item
系统创建： 12~15 个 Item
滚动时：
  Item 离开屏幕顶部 → 移动到屏幕底部 → 刷新数据 → 继续使用
  整个过程无 Instantiate、无 Destroy、无 GC
```

**完整实现（背包列表 1000 条数据流畅滚动）：**

```csharp
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class BagVirtualList : MonoBehaviour
{
    [Header("UI")]
    public ScrollRect scrollRect;
    public RectTransform content;
    public BagItem itemPrefab;

    [Header("Item Setting")]
    public float itemHeight = 100f;

    private List<BagItemData> dataList = new List<BagItemData>();
    private List<BagItem> itemList = new List<BagItem>();
    private int visibleCount;
    private int lastStartIndex = -1;

    void Start()
    {
        scrollRect.movementType = ScrollRect.MovementType.Clamped;
        CreateData();
        InitList();
        scrollRect.onValueChanged.AddListener(OnScroll);
    }

    void OnDestroy()
    {
        scrollRect.onValueChanged.RemoveListener(OnScroll);
    }

    void CreateData()
    {
        for (int i = 0; i < 1000; i++)
            dataList.Add(new BagItemData
            {
                Id = i,
                Name = "物品_" + i,
                Count = Random.Range(1, 999)
            });
    }

    void InitList()
    {
        // 计算可见数量（+2 缓冲）
        visibleCount = Mathf.CeilToInt(
            scrollRect.viewport.rect.height / itemHeight) + 2;

        // 只创建可见数量的节点
        for (int i = 0; i < visibleCount; i++)
        {
            BagItem item = Instantiate(itemPrefab, content);
            item.GetComponent<RectTransform>().sizeDelta =
                new Vector2(item.GetComponent<RectTransform>().sizeDelta.x, itemHeight);
            itemList.Add(item);
        }

        // Content 高度 = 总数据量 × 每项高度（让滚动范围正确）
        content.sizeDelta = new Vector2(content.sizeDelta.x, dataList.Count * itemHeight);

        RefreshItems(true);
    }

    void OnScroll(Vector2 pos) => RefreshItems();

    void RefreshItems(bool force = false)
    {
        float scrollY = Mathf.Max(0, content.anchoredPosition.y);
        int startIndex = Mathf.FloorToInt(scrollY / itemHeight);

        if (!force && startIndex == lastStartIndex) return;
        lastStartIndex = startIndex;

        for (int i = 0; i < itemList.Count; i++)
        {
            int dataIndex = startIndex + i;

            if (dataIndex < 0 || dataIndex >= dataList.Count)
            {
                itemList[i].gameObject.SetActive(false);
                continue;
            }

            BagItem item = itemList[i];
            item.gameObject.SetActive(true);

            // 移动到正确位置
            item.GetComponent<RectTransform>().anchoredPosition =
                new Vector2(0, -dataIndex * itemHeight);

            // 刷新数据
            item.Refresh(dataList[dataIndex]);
        }
    }
}
```

**性能对比**：

| 方案 | Hierarchy 节点数 | 内存 | GC | 滚动流畅度 |
|------|:--------------:|:---:|:--:|:---------:|
| 全量创建 1000 Item | 1000+ | 高 | 频繁 | ❌ 卡顿 |
| 虚拟列表 12 节点 | ~15 | 极低 | 几乎无 | ✅ 流畅 |

**动态高度列表**（聊天消息、评论等不等高 Item）：需要额外维护高度缓存数组 + 累积高度表，滚动定位用二分查找取代除法。

---

## 本章总结

```
UI 性能问题的五个维度：

① Batch（DrawCall）
   材质/纹理/Shader/Stencil 任一变化 → 断批
   诊断：Frame Debugger → Batch Breaking Reason

② 材质实例化
   给 graphic.material 赋新材质 / Mask 的 StencilMaterial.Add / 第三方插件 new Material
   注意：读取 graphic.material 不会实例化；Graphic 也没有 sharedMaterial
   诊断：Frame Debugger → Different Material

③ Mask/Stencil
   每层 Mask = 新材质 + 断批
   Mask 不减少 Overdraw
   诊断：Frame Debugger → Different Stencil State

④ 顶点膨胀
   Outline = ×5, Shadow = ×2, Text = 4 顶点/字
   诊断：Stats → Verts > 10K → 需要关注

⑤ Overdraw
   半透明混合无法 Early-Z
   诊断：Scene View → Overdraw Draw Mode

⑥ 工程层面（大规模项目）
   UI 分层架构（按层级拆 Canvas，隔离重建范围）
   对象池 / UI 缓存池（减少 Instantiate 和 GC）
   虚拟列表（只创建可见节点，循环复用）
   生命周期管理（标准化 Create/Show/Hide/Destroy）
```

本章与第 9~18 章的关系：

| 前几章的内容 | 本章的性能视角 |
|-------------|--------------|
| 第10章 渲染管线 | Canvas 重建成本、渲染顺序对 Batch 的影响 |
| 第15章 Mask 裁剪 | Stencil 断批、材质实例化、Overdraw 不减少 |
| 第17章 ModifyMesh | 顶点膨胀的来源（Outline = ×5, Shadow = ×2） |
| 第18章 UI Shader | Shader Keyword 断批、材质实例化、采样次数 × Overdraw |
| 第19章 特效实现 | 每个特效的采样次数和实际 GPU 成本 |

---

## 源码阅读路径

```
性能热点没有新文件：回到 CanvasUpdateRegistry.PerformUpdate、Graphic.DoMeshGeneration、
Text.OnPopulateMesh、VertexHelper.FillMesh、IMeshModifier 链，以及引擎 Canvas.BuildBatch。
用 Profiler 采样点（Canvas.SendWillRenderCanvases / Graphic.Rebuild / Canvas.BuildBatch）对照。
```

---

## 勘误汇总

| # | 严重程度 | 章节 | 原文声称 | 实际情况 |
|---|---------|------|---------|---------|
| 1 | 🔴 严重 | 20.2 | 「`material`（getter）：如果当前材质是共享材质，自动 `Instantiate()` 生成新实例再返回」「`graphic.material` 的 getter……会调用 `SetMaterialDirty()` 并创建材质的新实例」，并给出 `image.sharedMaterial` 的对比表与用法示例 | 这是 `Renderer.material` / `sharedMaterial` 的语义，被误安到了 `Graphic` 上。main `Graphic.cs` 中 getter 为 `return (m_Material != null) ? m_Material : defaultMaterial;`，**无任何副作用**；触发 `SetMaterialDirty()` 的是 setter。且 **`Graphic` 上不存在 `sharedMaterial` 属性**，原示例 ①③ 编译不过。本节已整体重写 |
| 2 | 🟡 中等 | 20.2 表格 | 「运行时改 Shader 参数 → `material.SetFloat()` 触发材质实例化」 | `graphic.material.SetFloat()` 不会实例化，而是直接修改共享材质，影响所有引用者——这是另一类（更隐蔽的）陷阱 |
| 3 | 🔴 严重 | 20.3 | 「RectMask2D……不产生材质实例化，**不影响 Batch**」 | 不实例化材质 ≠ 不断批。裁剪矩形与 `UNITY_UI_CLIP_RECT` 关键字本身是渲染状态：同一 RectMask2D 下可合批，跨 RectMask2D 断批。这与本章 20.1 合批条件表中已列出的「RectMask2D 裁剪矩形」自相矛盾，现已统一（全书口径见第 15 章） |
| 4 | 🟡 中等 | 20.3 | 表格断言「Fragment Shader ✅执行（结果被丢弃）→ Stencil Test 之后」 | 对 RectMask2D 成立（裁剪写在着色器内部），对 Mask 不成立：Stencil 测试是否 Early 取决于 GPU 与配置，见第 18 章 18.4.3。结论「不减少 Overdraw」不变，理由需分开说 |
| 5 | 🟢 轻微 | 20.4 | 顶点数风险分档（< 2K 安全 / 50K+ 灾难）未说明依据与适用平台 | 已补注：以中低端移动设备为参照的经验阈值，判断依据应为目标设备的 Profiler 采样 |

