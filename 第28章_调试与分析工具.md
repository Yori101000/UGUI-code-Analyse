# 第28章 调试与分析工具

> UGUI 的性能问题涉及布局、顶点生成、Canvas 重建、Batch 合批、GPU 渲染等多个环节，单靠经验很难准确定位。本章介绍 Frame Debugger、Profiler、RenderDoc 三种官方工具的使用方法，以及几个基于 UGUI 源码的自定义调试工具。

---

## 概述

UI 性能优化应建立在数据分析基础上，不是靠猜。Unity 提供了多层次的调试工具：

```
CPU 侧问题（重建、布局、顶点）
  → UI Profiler + Timeline

GPU 侧问题（DrawCall、Stencil、纹理切换）
  → Frame Debugger + RenderDoc

快速定位具体对象
  → 自定义工具（Canvas 重建监控、Dirty 追踪、RaycastTarget 检查）
```

---

## 28.1 Frame Debugger

Frame Debugger 逐帧查看所有渲染指令，是分析 UGUI 合批最直接的入口。

### 打开方式

```
Window → Analysis → Frame Debugger → Enable
```

Enable 后 Unity 暂停当前帧，左侧列表显示所有 DrawCall，右侧 Details 显示该 DrawCall 的渲染状态。

### 分析场景

**① 查看 UI DrawCall 数量和合批情况**

选中一个 DrawCall 后，查看 Details 中的材质、纹理、Shader。对比相邻两个 DrawCall 的 Details，**不同的字段就是 Batch 被中断的原因**：

```
DrawCall 1: Shader=UI/Default, Texture=Atlas_A, Stencil Ref=0
DrawCall 2: Shader=UI/Default, Texture=Atlas_A, Stencil Ref=1
                        ↑ 一致                ↑ Stencil Ref 变了 → Mask 断批
```

**② 查看渲染顺序**

UI 的提交顺序由 Hierarchy 层级决定。如果某个 UI 显示异常（该在前面的被遮住了），Frame Debugger 可以直接看到它是在哪个 DrawCall 里提交的，以及提交顺序是否正确。

**③ 分析 Mask 开销**

Mask 会产生额外的 Stencil 操作，在 Frame Debugger 中可以看到 Mask 相关 DrawCall 之前有明显的 Stencil Clear/Write 操作。可以通过这个观察 Mask 是否增加了不必要的 DrawCall。

### Frame Debugger 不能做什么

- 不能直接告诉你**是哪个 UI 元素**导致断批——只能看到材质/纹理状态不同，需要自己反查
- 不能看 CPU 重建耗时——那是 Profiler 的活

---

## 28.2 UI Profiler

### 打开方式

```
Window → Analysis → Profiler → 选择 UI 模块
```

### 核心指标

| 指标 | 反映的问题 |
|------|-----------|
| **Vertices** | UI 总顶点数。Text + Outline 组合下可能上万 |
| **Batches** | 当前 DrawCall 数 |
| **Layout Rebuild** | Layout 组件的递归计算次数。频繁触发 → 布局系统有问题 |
| **Graphic Rebuild** | 顶点数据重建次数。频繁触发 → 有 UI 在持续 Dirty |
| **SetVerticesDirty** | 谁在触发顶点重建 |
| **SetLayoutDirty** | 谁在触发布局重建 |

### CPU Timeline 关键函数

在 Profiler Timeline 视图中，关注以下函数：

| 函数 | 含义 |
|------|------|
| `Canvas.BuildBatch` | Canvas 正在重建 Batch。耗时高 → 顶点太多或 Canvas 太大 |
| `Canvas.SendWillRenderCanvases` | 当前帧所有 UI 重建的入口。耗时高 → Rebuild 太多 |
| `LayoutRebuilder.Rebuild` | Layout 递归计算。耗时高 → 布局嵌套复杂 |
| `Graphic.Rebuild` | 顶点数据重建。耗时高 → 大量 Graphic 被标记 Dirty |

### 分析流程

```
① 看 CPU 总耗时是否异常
② 看 Layout Rebuild 和 Graphic Rebuild 次数
③ 看 Batches 和 Vertices 数量
④ 在 Timeline 中定位具体耗时的函数
⑤ 用 Frame Debugger 验证渲染结果
```

---

## 28.3 RenderDoc

Frame Debugger 在 Unity 层面看渲染，RenderDoc 在 **GPU 驱动层面**看渲染——能看到实际提交给 GPU 的顶点数据、纹理、Shader 代码、Stencil 值。

### 安装与使用

1. 下载 RenderDoc（renderdoc.org）
2. Unity 中：Window → RenderDoc → 点击 Capture
3. 自动跳转到 RenderDoc 界面，查看当前帧的完整 GPU 命令

### 关键功能

**Event Browser**——看到所有 GPU DrawCall，包括 Unity 不显示的 Clear、Stencil 操作。

**Mesh Viewer**——查看每个 DrawCall 实际提交的顶点数据：

- 顶点位置、UV、颜色
- 可以验证 UGUI 的顶点生成是否正确（比如 Outline 是否真的复制了顶点）
- 可以看到 Batch 合并后顶点是怎么排列的

**Texture Viewer**——查看该帧使用的所有纹理。

- 验证 SpriteAtlas 是否正常打包
- 查看 Font Atlas 的实际情况和 UV 映射

**Pipeline State**——查看完整的 GPU 渲染状态：

- Stencil Ref/Comp/Pass（验证 Mask 的 Stencil 配置）
- Blend 模式
- Shader 实际代码

### Frame Debugger vs RenderDoc

| | Frame Debugger | RenderDoc |
|------|------|------|
| 层面 | Unity 渲染层 | GPU 驱动层 |
| DrawCall | ✅ 可以看 | ✅ 更详细 |
| Batch 断因 | ✅ 直接提示 | ❌ 需要自己对比状态 |
| 顶点数据 | ❌ 不可看 | ✅ 可看 |
| Stencil 值 | ❌ 不可看 | ✅ 可看 |
| 纹理内容 | ❌ 不可看 | ✅ 可看 |
| Shader 代码 | ❌ 不可看 | ✅ 可看 |
| 操作复杂度 | 简单，一键开启 | 需要安装、捕获 |

**配合使用**：Frame Debugger 快速定位问题 → RenderDoc 深入分析 GPU 细节。

---

## 28.4 自定义调试工具

官方工具能告诉你"有问题"，但不总能告诉你是"哪个对象"出的问题。以下工具基于 UGUI 源码的关键节点实现，用于快速定位具体对象。

### 28.4.1 Canvas 重建监控

利用 `Canvas.willRenderCanvases` 事件（Canvas 重建前触发），统计每秒重建次数：

```csharp
public class CanvasRebuildMonitor : MonoBehaviour
{
    private int rebuildCount;
    private float timer;

    private void OnEnable()
    {
        Canvas.willRenderCanvases += OnWillRenderCanvases;
    }

    private void OnDisable()
    {
        Canvas.willRenderCanvases -= OnWillRenderCanvases;
    }

    private void OnWillRenderCanvases()
    {
        rebuildCount++;
    }

    private void Update()
    {
        timer += Time.deltaTime;
        if (timer >= 1f)
        {
            Debug.Log($"Canvas Rebuild Count : {rebuildCount}");
            rebuildCount = 0;
            timer = 0f;
        }
    }
}
```

静态界面重建次数应该稳定。如果持续增长 → 有 UI 在不停触发 Dirty。

### 28.4.2 Graphic Dirty 追踪

继承 `Image` 并重写 Dirty 方法，监控谁在每帧触发重建：

```csharp
public class DebugImage : Image
{
    public override void SetVerticesDirty()
    {
        Debug.Log($"{name} SetVerticesDirty", this);
        base.SetVerticesDirty();
    }

    public override void SetMaterialDirty()
    {
        Debug.Log($"{name} SetMaterialDirty", this);
        base.SetMaterialDirty();
    }
}
```

在控制台看到类似：

```
HPBar SetVerticesDirty
HPBar SetVerticesDirty
HPBar SetVerticesDirty
```

如果每帧都在打印 → 这个对象在持续触发重建。常见原因：每帧改 text、每帧改 color、无意义的属性赋值。

### 28.4.3 RaycastTarget 检查

扫描所有没必要的 RaycastTarget（不是 Button 但开启了射线检测的 Graphic）：

```csharp
public class RaycastTargetChecker : MonoBehaviour
{
    [ContextMenu("Check RaycastTarget")]
    private void Check()
    {
        Graphic[] graphics = FindObjectsOfType<Graphic>(true);
        foreach (var g in graphics)
        {
            if (!g.raycastTarget) continue;
            if (g.GetComponent<Button>()) continue;
            Debug.LogWarning($"Unused RaycastTarget: {g.name}", g.gameObject);
        }
    }
}
```

执行后输出类似：

```
Unused RaycastTarget: Background
Unused RaycastTarget: Frame
Unused RaycastTarget: DecorationText
```

关闭这些无意义的 RaycastTarget 是 Raycast 检测优化收益最高的单项操作。

### 28.4.4 Batch 风险统计

快速统计界面中的材质和纹理数量——数量过多往往意味合批效果差：

```csharp
Graphic[] graphics = FindObjectsOfType<Graphic>(true);
HashSet<Material> materials = new HashSet<Material>();
HashSet<Texture> textures = new HashSet<Texture>();

foreach (var g in graphics)
{
    materials.Add(g.material);
    if (g.mainTexture != null) textures.Add(g.mainTexture);
}

Debug.Log($"Material Count: {materials.Count}, Texture Count: {textures.Count}");
```

---

## 本章总结

```
UI 调试工具链：

发现问题范围
  → UI Profiler（CPU：Rebuild、Batch、顶点数）

定位渲染原因
  → Frame Debugger（Batch 断因、Mask 开销、渲染顺序）

深入 GPU 细节
  → RenderDoc（顶点数据、Stencil 值、纹理内容）

锁定具体对象
  → 自定义工具（Dirty 追踪、Canvas 重建监控、RaycastTarget 检查）
```
