# 全库审查报告（2026-08-14）

> 审查目标：在《源码一致性审计报告（2026-08）》之外，做一次**覆盖全部 25 章 + 全部 `_阅读指南` 文档**的复审，重点找**跨章互相矛盾的事实性错误**、**编造 API**、**不可编译的示例代码**、**无测量条件的性能数字**，以及**元文档自身的失真**。
>
> 判定基线（同 `AGENTS.md`）：uGUI 仓库以 **main 分支**为准（`com.unity.ugui/Runtime/UGUI/...`）；引擎内置类型以 Unity 官方文档为准；引擎内部行为（C# 无源码）标注为「行为推断」。
>
> **本报告只记录问题，不修改任何章节正文。** 修哪些、什么时候修由维护者决定。

---

## 与《源码一致性审计报告（2026-08）》的关系

上一次审计声称「范围：全部 25 章 + `_阅读指南`」，但其明细表实际只落在 **01/03/04/06/08/09/16/17/19** 九章。本次复审发现：它宣称要找的两类问题（编造 API、引擎归属错误）在 **04 章 4.6 节**和 **20 章 20.2 节**大量存在却未被检出（见 B1、B2）。

因此本报告的定位是：**对上一次审计未覆盖区域的补充**，而不是重复劳动。两份报告应并存，上一份的 21 项结论仍然有效。

---

## 审查方法

- 通读全部 25 章正文 + `_阅读指南/` 全部 4 份文档
- 对关键论断回查原始来源：
  - uGUI main `com.unity.ugui/Runtime/UGUI/UI/Core/Graphic.cs`
  - uGUI main `com.unity.ugui/Runtime/UGUI/UI/Core/RectMask2D.cs`
  - 引擎内置 `UI-Default.shader`
- 交叉比对同一事实在不同章节的表述，凡结论相反者一律记为矛盾（不预设哪一方正确，但给出源码判定）

---

## 结论摘要

| 类别 | 条目数 | 优先级 |
|---|:---:|:---:|
| A. 跨章事实矛盾 | 12 | **P0** |
| B. 编造 / 错误 API | 5 | **P0** |
| C. 示例代码不可编译或有 bug | 9 | P1 |
| D. 无测量条件的性能数字 | 5 | P1 |
| E. 勘误机制形式化 / 元文档失真 | 5 | **P0** |
| F. 结构与体例 | 7 | P2 |
| G. 内容增补机会（非错误） | 4 | P2 |
| **合计** | **47** | |

三句话判断：

1. **主线可信**。01/02/05/09/10/11/12/15/17 章经抽查与 main 一致，其中 05.4.2 `SetVerticesDirty()`、05.4.4 `SetLayoutDirty()` 的方法体与 main **逐行一致**，02.7.2 的 `s_VertexHelper` / `Allocator.Domain` 描述准确。
2. **问题集中在"衍生章节"与元文档**：04/06/08/18/20/24 章和 `_阅读指南/`。这些正是上一次审计未真正覆盖的部分，也恰恰是读者最常直接引用的部分（性能、案例、导读）。
3. **最大的单点风险不是某个错误，而是 `_阅读指南/第1-8章源码勘误.md`** —— 它是一份"验证报告"，却把本书后来已标为 🔴 的错误认证为「✅ 一致」（见 E2）。读者若先读它，会对全书可信度形成错误判断。

---

## A. 跨章事实矛盾（P0）

同一事实在不同章节结论相反。这类问题比孤立错误更严重：读者无法自行判断该信哪一个。

| # | 严重 | 主题 | 各章表述 | 实际情况 / 判定 | 证据来源 | 建议改法 |
|---|:---:|---|---|---|---|---|
| A1 | 🔴 | **RectMask2D 的机制与断批** | 全书**四种互斥说法**：<br>① 09.4.4「RectMask2D 通过修改顶点位置实现裁剪」<br>② 08.5.2 / 15.2.3 / 15.3.2 / 20.3 / 24.5.3「不断批」<br>③ 15.2.1 / 15.3.3「硬件 Scissor Test，固定功能管线」<br>④ 09.4.4 / 20.1 / 导读第 09 章条目「不同 RectMask2D 会断批」 | **机制**：`RectMask2D` → `IClippable.SetClipRect()` → `MaskableGraphic` → `canvasRenderer.EnableRectClipping(rect)`；**不修改任何顶点**。GPU 侧由 `UI-Default.shader` 的 `UNITY_UI_CLIP_RECT` keyword + `_ClipRect` 实现，**是 shader 裁剪，不是硬件 Scissor**。<br>**断批**：同一 clipRect 下的 Graphic 可合批；不同 clipRect / keyword 开关不同的元素之间会断批（Frame Debugger 显示 `Different RectMask2D`）。<br>→ ① 机制错，③ 机制错，② 断批结论错，④ 断批结论正确但机制描述错 | main `RectMask2D.cs`（`SetClipRect` / `Cull` / `SetClipSoftness`，无顶点操作）<br>`UI-Default.shader`：`#pragma multi_compile_local _ UNITY_UI_CLIP_RECT` + `color.a *= UnityGet2DClipping(IN.worldPosition.xy, _ClipRect);`<br>断批结论属**行为推断**（Frame Debugger 观察 + 20.2 自述的 keyword 断批规则） | 统一到一段标准表述，在 08/09/14/15/20/24 六处替换：「RectMask2D 通过 `CanvasRenderer.EnableRectClipping` 设置裁剪矩形，由 UI Shader 的 `UNITY_UI_CLIP_RECT` + `_ClipRect` 在片元阶段裁剪，不修改顶点、不创建材质实例；同一裁剪矩形内的元素可以合批，不同 RectMask2D 之间会断批（行为推断，Frame Debugger 可验证）」 |
| A2 | 🔴 | **`Graphic.material` 的语义** | 20.2 整节：「material（getter）：如果当前材质是共享材质，自动 `Instantiate()` 生成新实例再返回」「`graphic.material` 的 getter 在检测到材质可能被修改时，会调用 `SetMaterialDirty()` 并创建材质的新实例」，并给出 `image.sharedMaterial` 用法<br>10.5.4 勘误 #7 / 汇总 #8 却正确指出「`materialForRendering` 是只读 getter，访问不创建实例」 | main `Graphic.cs`：<br>`get { return (m_Material != null) ? m_Material : defaultMaterial; }`<br>`set { if (m_Material == value) return; m_Material = value; SetMaterialDirty(); }`<br>**getter 无任何副作用，不实例化**；调 `SetMaterialDirty()` 的是 setter。<br>`Graphic` **没有 `sharedMaterial` 属性**（它不是 `Renderer`）。<br>→ 20.2 把 `Renderer.material` / `sharedMaterial` 的语义误安到了 `Graphic` 上，整节立论错误 | main `Graphic.cs` 的 `material` 属性 | 20.2 整节重写：按 `Graphic.material` 真实语义（getter 无副作用、setter 触发 `SetMaterialDirty`）+ `materialForRendering` 的 `IMaterialModifier` 链 + `StencilMaterial.Add()` 讲材质实例化的真实来源；删除全部 `sharedMaterial` 示例；20 章勘误表补两行 |
| A3 | 🔴 | **`DoMeshGeneration` 的实现** | 04.5.2 仍是旧写法：`using (VertexHelper vh = new VertexHelper())` + `s_MeshPool.Get()/Release()`<br>02.7.2 与 05.5.3 已按 main 更正为静态共享实例 | main：`private static readonly VertexHelper s_VertexHelper = new VertexHelper(Allocator.Domain);`，配合静态 `workerMesh`，**不存在每次 new 与 MeshPool 出入池** | main `Graphic.cs`（已实测回查） | 04.5.2 按 05.5.3 的版本同步；04 章勘误表补一行 |
| A4 | 🟡 | **`UpdateMaterial` 的写法** | 04.5.3：`GetModifiedMaterial(m_Material)` + `canvasRenderer.SetMaterial(mat, 0)`<br>05.5.4 / 08.3.2 已更正为 `materialCount = 1` + `SetMaterial(materialForRendering, 0)` | 以 05.5.4 / 08.3.2 为准 | 08 章勘误表 #2（已记录同类问题） | 04.5.3 同步；04 章勘误表补一行 |
| A5 | 🟡 | **`FillMesh` 的上传方式** | 18.2 仍写 `Mesh.vertices = ... / Mesh.colors32 = ... / Mesh.uv = ...` 逐通道赋值<br>02.7.5 已更正为 `SetVertexBufferParams`（9 通道）+ 单次 `SetVertexBufferData` 单流上传 | 以 02.7.5 为准 | 02 章勘误表 #4（已记录同类问题） | 18.2 的链路图同步；18 章勘误表补一行 |
| A6 | 🟡 | **Stencil / 裁剪与 Fragment Shader 的先后** | 15.3.3「被裁剪的像素仍然执行片元着色器，裁剪（Stencil 或 Scissor）发生在着色器之后」<br>20.3 用表格断言「Fragment Shader ✅执行（结果被丢弃）→ Stencil Test 之后」<br>18.4.3 明确反驳：「现代 GPU 支持 Early Stencil Test……不能一概说『Stencil Test 在 Fragment Shader 之后』」 | 18.4.3 的表述更准确：是否 Early 取决于 GPU 架构与 Stencil 操作配置（写入操作 `Replace` 可能阻止 Early）。<br>15.3.3 / 20.3 把一个**依赖硬件的条件行为**写成了确定结论，且用表格形式加强了确定性 | 18 章勘误表 #2（本书自述）；属**行为推断**，无 C# 源码可证 | 15.3.3 与 20.3 改为与 18.4.3 一致的条件表述，并标「行为推断」；「Mask/RectMask2D 都不减少 Overdraw」这一结论本身可保留，但理由要改为"裁剪不减少已提交的片元覆盖面积"，而非"Stencil 在 FS 之后" |
| A7 | 🔴 | **Mask 内子元素能否互相合批** | 15.1.7 / 15.3.1 用整节论证：`StencilMaterial.Add()` 有缓存，相同 Stencil 参数返回同一实例，**同深度子元素共享材质、内部可以合批**，Batch 数 = 不同 Stencil 参数组合数<br>24.5.3 案例三却以「每一个在 Mask 内的 UI 元素都使用了不同的 Stencil Ref，导致每个元素单独一个 DrawCall」为前提 | 以 15 章为准 —— 15 章正文就是专门为纠正这个常见误解写的。24.5.3 的 Frame Debugger 列表呈现 `Ref=0 / Ref=2` 交替，实为 **Hierarchy 顺序导致的穿插断批**，不是"每个元素一个 Ref" | main `StencilMaterial.cs` 的缓存 key（15 章已引用） | 24.5.3 整个案例重写：现象保留，根因改为「Mask 内外元素在 Hierarchy 中交替排列 → 渲染顺序穿插 → 同材质也被拆批」；结论表的 15→8→3 需重新说明或改为定性 |
| A8 | 🟡 | **`Canvas.willRenderCanvases` 的委托签名** | 06.6.1：`public static event Action<float> willRenderCanvases;`（带 float 参数）<br>04.2.1：`Canvas.willRenderCanvases += PerformUpdate;`（`PerformUpdate()` 无参）<br>21.4.1：`Canvas.willRenderCanvases += OnWillRenderCanvases;`（`OnWillRenderCanvases()` 无参） | 引擎实际为**无参**委托（`public delegate void WillRenderCanvases();`）。04 / 21 章的用法正确，06.6.1 的声明错误 | Unity 官方文档 `Canvas.willRenderCanvases`；04 / 21 章自身用法反证 | 06.6.1 改为无参委托；06 章勘误表补一行 |
| A9 | 🟡 | **Graphic 何时获取 CanvasRenderer** | **同一章内自相矛盾**：08.3.3 已按 main 更正为惰性属性（首次访问才 `GetComponent`/`AddComponent`），并在勘误表 #3 记录；但 08.3.4 的生命周期表格仍写「Awake ｜ 获取 `GetComponent<CanvasRenderer>()` ｜ 建立引用」，8.7 生命周期第 3 步仍写「`Graphic.Awake()` → `GetComponent<CanvasRenderer>()` 建立引用」 | 以 08.3.3 为准（main 的惰性 `canvasRenderer` 属性） | main `Graphic.cs`；08 章勘误表 #3 | 08.3.4 与 8.7 同步；这是上次审计"改了一处漏了两处"的典型，修时需全章 grep `Awake` |
| A10 | 🟢 | **Outline + Shadow 的顶点数** | 17.2.2 表格：1000 字 + Outline + Shadow = **28000**<br>20.4：4000 → Outline ×5 = 20000 → Shadow ×2 = **40000**<br>17.2.3③ 自述的乘法规则也导向 40000 | 按本书自述的乘法规则，两种叠加顺序都得 40000。17.2.2 的 28000 无法由文中任何规则推出 | 两章互推 | 17.2.2 表格改为 40000，或补充 28000 的推导前提 |
| A11 | 🟡 | **UI Shader 的 Blend 模式** | `_阅读指南/UGUI源码导读.md` 第 10 章、第 18 章条目**两处**写 `Blend One OneMinusSrcAlpha`（预乘 Alpha）<br>正文 10.4.2 / 18.1 / 18.3.2 / 19 章全部 Shader 均为 `Blend SrcAlpha OneMinusSrcAlpha` | 正文正确。`UI-Default.shader` 实为 `Blend SrcAlpha OneMinusSrcAlpha`；`One OneMinusSrcAlpha` 是 18.3.2 表格中列出的 **Premultiplied Alpha** 变体，不是默认值 | `UI-Default.shader` 实际内容（已回查） | 导读两处改为 `Blend SrcAlpha OneMinusSrcAlpha` |
| A12 | 🟢 | **TMP 的 SDF 参数走哪个通道** | 导读第 16 章条目**一句话内自相矛盾**：「TMP：SDF 图集 + 字符缓存，`uv2` 传 SDF 参数（需开启 Canvas 的 `additionalShaderChannels` 的 **TexCoord1**）」<br>06.9.2 与 16.4.1 均为 **TexCoord2** | 以 06.9.2 / 16.4.1 为准：`uv2` ↔ `TexCoord2` | 两章互推 | 导读改为 TexCoord2 |

---

## B. 编造 / 错误 API（P0，源码可证）

| # | 严重 | 位置 | 原文声称 | 实际情况 | 建议改法 |
|---|:---:|---|---|---|---|
| B1 | 🔴 | 04.6.1 / 04.6.2 | 两张「触发链」表列出的方法：`Graphic.SetColor()`、`Image.SetSprite()`、`Image.SetType()`、`Text.SetText()`、`Text.SetFontSize()`、`Graphic.SetMaterial()` | **uGUI 中均不存在这些方法**。实际都是属性 setter，内部经 `SetPropertyUtility.SetStruct/SetClass/SetColor` 判断变化后再调 `SetVerticesDirty()` / `SetAllDirty()` 等。（`SetText` 是 TMP 的 API，不是传统 `Text` 的）<br>→ 这正是上一次审计定义的「编造 API」类型，而 04 章在该审计的名单内却漏检 | 两张表的「源码位置」列全部改为属性 setter 形式，例如 `Graphic.color` setter → `SetVerticesDirty()`；04 章勘误表补一行，注明整表修正 |
| B2 | 🔴 | 20.2 | `image.sharedMaterial`（出现在对比表与示例 ①③ 中） | `Graphic` 无 `sharedMaterial` 属性，**代码编译不过**。读者照抄示例 ③ `image.sharedMaterial.color = Color.red;` 会直接报错 | 随 A2 一并重写 20.2 |
| B3 | 🟡 | 04.5.2 / 05.5.3 | `private void UpdateGeometry()` | main 为 `protected virtual void UpdateGeometry()`，且含 `useLegacyMeshGeneration` 分支（该分支现仅 `Debug.LogError`）。<br>访问修饰符影响"子类能否 override"的判断，而 05.9 正好在教读者自定义 Graphic | 改为 `protected virtual`；简化掉 legacy 分支可以，但加一句说明 |
| B4 | 🟢 | 06.4.1 | `public int cached SortingLayerValue;` | 标识符含空格，**非法 C#**。同文件 6.6.1 处写法正确（`cachedSortingLayerValue`） | 改为 `cachedSortingLayerValue` |
| B5 | 🟡 | 08.4.4 | 「除了 Native 方法，CanvasRenderer 也在 C# 侧封装了一些属性，这些属性**完全由 C# 管理，不涉及 Native 同步**」，随后列出 `cull` / `absoluteDepth` / `materialCount` / `hasMoved` | 这四个均为引擎侧属性（extern），由 Native 维护。8.4.2 自己刚论证过"数据直接存储在 Native 侧"，此处结论与之矛盾 | 删除"不涉及 Native 同步"的说法，改为"C# 侧可读写、实际状态由引擎维护" |

---

## C. 示例代码不可编译或有 bug（P1）

| # | 严重 | 位置 | 问题 | 建议改法 |
|---|:---:|---|---|---|
| C1 | 🟡 | 18 章、19 章**全部** Shader 示例 | 只 `#include "UnityUI.cginc"`，却使用了 `UnityObjectToClipPos`、`TRANSFORM_TEX`、`appdata_base`、`ComputeGrabScreenPos` —— 这些都来自 `UnityCG.cginc`。按原文抄进工程**无法编译** | 所有示例补 `#include "UnityCG.cginc"`（`UI-Default.shader` 实际两个都 include） |
| C2 | 🔴 | 18.1 | 号称「直接看**完整代码**逐行拆解」的 `UI/Default`，实际缺：`_ClipRect`、`_TextureSampleAdd`、`_ColorMask`、`_UseUIAlphaClip` 四个属性；`UNITY_UI_CLIP_RECT` / `UNITY_UI_ALPHACLIP` 两个 keyword；`worldPosition` 插值量；片元里的裁剪与 alpha clip 分支 | **缺掉的正好是 RectMask2D 的实现机制**——这与 A1 的全书混乱互为因果。建议要么补全，要么把「完整代码」改成「主干简化版」并明确列出略去了什么、其中 `UNITY_UI_CLIP_RECT` 部分见第 15 章 |
| C3 | 🟡 | 19.7.2 | 毛玻璃号称「双 Pass 高斯模糊……复杂度从 O(N²) 降到 O(2N)（7+7=14 次）」，但 `GrabPass` 在 SubShader 级只抓一次，**两个 Pass 都采样同一张 `_GrabTexture`**，Pass 2 读不到 Pass 1 的输出 → 不构成可分离模糊，实际是两次独立单向模糊叠加输出 | 要么补第二个 `GrabPass` / RenderTexture 中转，要么删掉"等价 14 次采样"的性能结论，改述为示意 |
| C4 | 🟢 | 17.2.2 | Outline 示例中 `ApplyShadow(verts, effectColor, startIndex, verts.Count, ...)` 使用了**从未声明**的 `startIndex`（只声明了 `originalCount`） | 补声明或改用 `originalCount` |
| C5 | 🟡 | 24.2.2 | 虚拟列表 `Refresh()` 只判断 `dataIndex >= dataList.Count`，**缺 `dataIndex < 0` 保护**；且 `startIndex` 计算无 `Mathf.Max(0, ...)` 钳制。Elastic 回弹或顶部过滚时 `content.anchoredPosition.y` 为负 → 数组越界。20.6.4 的同类实现两个保护都有 | 与 20.6.4 对齐；本章自称「代码全部可运行」，更需修正 |
| C6 | 🟢 | 23.3 | `DOFloat(float3.DOScaleX(2, 1)` —— 括号不闭合、语义不明，疑似编辑事故 | 重写该示意块 |
| C7 | 🟢 | 07.4 | 代码块内混入中文伪代码：`float targetDPI = m_PhysicalUnit 对应的值;` | 改为 `switch (m_PhysicalUnit)` 的真实分派，或移出代码块改为文字说明 |
| C8 | 🟢 | 19.1.3 | `VertexGradient.ModifyMesh` 中 `if/else` 分支缩进脱出 for 循环层级，视觉上像在循环外 | 修正缩进 |
| C9 | 🟢 | 24.3.3 | `Close<T>() where T : UIPanel` 的类型参数 **T 完全未使用**，实际按栈顶弹出，与签名承诺不符（`Close<ConfirmPanel>()` 可能关掉别的面板）；同时 `Update` 用 `Input.GetKeyDown`（旧输入系统），与 12.6 的新输入系统章节脱节 | 去掉无用泛型或改为按类型查找；`Input` 用法加一句版本说明 |

---

## D. 无测量条件的性能数字（P1）

**处理方针（已确认）：改为定性描述。** 删除具体数值，改为量级或方向性表述；确需保留数值的，必须标注为某次具体测量并附环境（Unity 版本 / 设备 / Editor 或 Build）。

| # | 严重 | 位置 | 问题 | 建议措辞 |
|---|:---:|---|---|---|
| D1 | 🔴 | 25.3.1 | 「创建 1000 个 Image：UGUI ~8-15ms / UI Toolkit ~2-5ms」等三行硬数字，**无 Unity 版本、设备、Editor/Build、是否含 Awake 的任何说明**。选型章最容易被截图当决策依据，风险最高 | 改为：「元素实例化：UI Toolkit 的 VisualElement 不是 GameObject，创建与销毁开销显著低于 UGUI；批量创建/销毁场景下差距会随数量放大。具体数值随 Unity 版本与设备差异很大，选型前应在目标设备实测。」 |
| D2 | 🟡 | 23.6 | 「性能实测」表：`~8ms GC Alloc` —— **用时间单位描述分配量**，单位错误；且无测量环境，"实测"二字无依据 | 小节改名为「性能特征对比」，改为定性：「手写协程每次启动都产生托管分配；DOTween 通过对象池复用 Tween 对象，稳态下接近零分配。」 |
| D3 | 🟡 | 06.9.3 | 带宽表：None（pos+color+uv0）= **32B**。按 position(12) + color(4) + uv0(8) 应为 24B，32 这个数字来源不明，后续 48/64/76/92 及百分比均建立其上 | 要么给出计算口径（是否含对齐、uv0 按 Vector2 还是 Vector4），要么整表改为相对倍数（+50% / +100%）不给绝对字节数 |
| D4 | 🟡 | 24.5.1 ~ 24.5.3 | 三个案例的 Profiler 数字（`LayoutRebuilder.Rebuild` 8.2ms、总帧 12ms→3ms、DrawCall 15→8→3）**无任何环境说明**；且 24.5.3 的分析前提本身有 A7 的错误 | 保留案例结构与诊断流程（这是本节真正的价值），把数字改为定性描述或明确标注"某项目某设备的一次实测，仅作量级参考" |
| D5 | 🟢 | 20.4 | 顶点数风险分档（< 2K 安全 / 2K~10K 注意 / 10K~50K 严重 / 50K+ 灾难）**无依据、无适用平台** | 加一句限定：「以中低端移动设备为参照的经验阈值，桌面端可放宽；实际以目标设备 Profiler 为准。」 |

---

## E. 勘误机制形式化 / 元文档失真（P0）

这一类不影响单条知识的正确性，但直接影响**读者对全书可信度的判断**，因此列为 P0。

| # | 严重 | 位置 | 问题 | 建议改法 |
|---|:---:|---|---|---|
| E1 | 🟡 | 07 / 13 / 20 / 21 / 22 / 23 / 24 / 25 章 | 勘误表为占位行「本章无源码级勘误记录」。其中 **20 章那一条已被 A2 / B2 直接证伪**（该章有一整节立论错误）。空表比没有表更误导——读者会理解成「这一章已经核查过了」 | 三选一：① 改为「已核查项清单」，明确写核查了哪些论断；② 若未真正核查，删除占位行，只保留标题；③ 保留占位行但改措辞为「本章尚未做逐条源码核查」。**不建议维持现状** |
| E2 | 🔴 | `_阅读指南/第1-8章源码勘误.md` | **一份过期且反向错误的"验证报告"**，是全库可信度的最大单点风险：<br>① 开篇「源码基于 Unity 2019.4/2020.3 LTS 版本」——直接违反 `AGENTS.md` 的 main 基线强制条款；<br>② 把「UIVertex 结构体定义在 VertexHelper.cs 中」认证为「✅ 一致」——而 02 章勘误 #1 已把这条标为 🔴；<br>③ 把 CanvasRenderer 章认证为「**无源码级错误**」——而 08 章现有 4 条勘误（含 1 条 🔴）；<br>④ 把 Graphic 章认证为「无源码级错误」——而 05 章现有 2 条勘误；<br>⑤ 第 6 章「补充：排序规则」的 Overlay 优先级写成「`sortingOrder` → Hierarchy」，**漏掉 Sorting Layer**，与 06.4.2 冲突；<br>⑥ 结论句「第1~8章……未发现需要修正的严重错误。这与第9~29章中发现的多处严重错误形成对比」——既用旧编号，也已被 2026-08 审计（01/03/08/09 均有 🔴）证伪。<br>而 `agent.md` 第 4 节只把《整体结构分析》标为历史存档，**没有标记本文件** | 建议废弃或加显著过期标注。若保留，须在文首加与《整体结构分析（历史存档）》同级的警示框，并在 `agent.md` 第 4 节登记 |
| E3 | 🟡 | `_阅读指南/源码一致性审计报告.md` | 「审查范围与方法」声称「范围：全部 25 章 + `_阅读指南`」，但明细表只落在 01/03/04/06/08/09/16/17/19 章，且结论写「第 02/04/05/07 等章复查未发现同类问题」。本次复审在 04.6（B1）与 20.2（B2）发现的正是它宣称要找的类型 → **实际覆盖面窄于声明** | 修正该报告的范围表述为实际覆盖的九章，并注明 20/22~25 章未覆盖、由本报告补充 |
| E4 | 🟡 | `agent.md` 第 4 节 | **第 2 条与第 7 条互相矛盾**：第 2 条说《UGUI源码导读.md》「仍按旧编号编写……待按新结构重写」；第 7 条说「已按新编号重写」。查该文件本身已写明「本文档按新编号 01~25 编写」，第 2 条是失效残留 | 删除第 2 条或与第 7 条合并 |
| E5 | 🟢 | `agent.md` 第 0 节 | 「共 31 个 `.md` 文档（约 490KB）」——本次改动前实际为 32 个（约 560KB） | 更新计数与体积 |

---

## F. 结构与体例（P2）

| # | 严重 | 位置 | 问题 | 建议改法 |
|---|:---:|---|---|---|
| F1 | 🟡 | 24 章引言 | 「本章将前 **28 章**的知识点应用到三个完整案例中」——全书仅 25 章，旧编号残留。`agent.md` 明确规定文件名编号是唯一可信来源 | 改为「前 23 章」或「前面各章」 |
| F2 | 🟢 | 03 章 | 小节编号错位：`### 6.4 跨 Canvas 空间转换` 挂在 `## 3.6` 之下，应为 `3.6.4` | 改号 |
| F3 | 🟢 | 02 章 2.9 | 「**下一章预告**：……这就是**第9章** 批处理与DrawCall 即将讨论的问题」——下一章是第 3 章，预告指向错误（重构前 02 的下一章确是批处理） | 改为「后续第 9 章」或删除"下一章"字样 |
| F4 | 🟢 | 02 章 2.5.1 | UV 示意图把 `(0,0)` 标在左上、`(0,1)` 标在左下，与紧邻代码上下颠倒（代码中 `(0,0)` 对应 `r.yMin`，即左下） | 翻转示意图标注 |
| F5 | 🟢 | 全库 | 收尾体例三种混用：`## 本章总结`（11/12/14/15/16 等）/ `## N.x 本章总结`（01/05/09 等）/ `## 总结`（03/04）。「源码阅读路径」段位置也不统一：多数在勘误汇总之前，21/24/25 章在之后 | 统一为固定顺序：`## 本章总结` → `## 源码阅读路径` → `## 勘误汇总`，并在 `agent.md` 第 3 节的章节模板中写死 |
| F6 | 🟢 | 全库 | 颗粒度失衡：25 章 5.7KB vs 19 章 33.5KB / 10 章 33.4KB / 11 章 32.8KB。`agent.md` 第 4 节已自述此问题 | 按 `agent.md` 第 8 节建议做章内重排，不拆文件（避免全库交叉引用失效） |
| F7 | 🟢 | 01/03/05/06/07/08/09 章引言 | 「本章对应原书结构中的第 N 章」——`agent.md` §3 解释了这是重构前旧编号，但**正文本身无任何提示**，读者独立阅读时极易误判当前章节号（07 章尤甚：正文说"对应原书第 2 章"） | 统一改为脚注或加一句「（此处指 2026-08 重构前的旧编号，当前章号以文件名为准）」，或直接删除这些历史说明 |

---

## G. 内容增补机会（P2，非错误）

- **25 章过薄**（5.7KB，全书最短）。可补：UI Toolkit 的实际渲染路径（不走 Canvas.BuildBatch）、运行时限制、与 UGUI 混用时的排序与事件边界。目前 25.5「共存策略」只有 3 条要点。
- **19 / 20 章过大**（33.5KB / 21KB），按 `agent.md` 第 8 节建议做章内重排而非拆文件。
- **主题空白**：
  - `CanvasGroup` 无专门讲解，内容散在 04.8.2（alpha 不触发 Rebuild）、18.3.1（Alpha 来源链）、13.4（IsInteractable）、22 章。它是 UI 性能与交互控制的高频组件，值得一节。
  - `Image` 的 Sliced / Tiled / Filled 三种 Type 的 Mesh 生成只有 2.5.2 / 2.5.3 各一段，与 Simple 的详细程度不对称。
  - `Dropdown` / `InputField` 在 13 章仅出现在继承树中，无任何机制说明。
- **仓库无 LICENSE**（`agent.md` 第 0 节已自述）。

---

## 按章索引

便于逐章施工。括号内为该条在上表中的编号。

| 章 / 文件 | 涉及条目 | 数量 |
|---|---|:---:|
| 02 章 | F3、F4 | 2 |
| 03 章 | F2 | 1 |
| **04 章** | A3、A4、**B1** | 3 |
| 05 章 | B3 | 1 |
| 06 章 | A8、B4、D3 | 3 |
| 07 章 | C7、E1 | 2 |
| **08 章** | A1、**A9**、B5 | 3 |
| **09 章** | **A1** | 1 |
| 13 章 | E1 | 1 |
| 14 章 | A1 | 1 |
| **15 章** | A1、A6 | 2 |
| 17 章 | A10、C4 | 2 |
| **18 章** | A5、**C1**、**C2** | 3 |
| 19 章 | C1、C3、C8 | 3 |
| **20 章** | **A1**、**A2**、A6、**B2**、D5、E1 | 6 |
| 21 章 | E1 | 1 |
| 22 章 | E1 | 1 |
| 23 章 | C6、D2、E1 | 3 |
| **24 章** | **A1**、**A7**、C5、C9、D4、E1、F1 | 7 |
| 25 章 | **D1**、E1、G | 3 |
| 全库 | F5、F6、F7 | 3 |
| `UGUI源码导读.md` | A11、A12 | 2 |
| **`第1-8章源码勘误.md`** | **E2** | 1 |
| `源码一致性审计报告.md` | E3 | 1 |
| `agent.md` | E4、E5 | 2 |

**改动量最大的三处：24 章（7 条）、20 章（6 条）、04/06/08/18/19 各 3 条。**

---

## 修复优先级建议

### 第一批 P0（建议优先，收益最高）

**A + B + E**，共 22 条。理由：

- **A2 / B2（20.2 整节）** 和 **B1（04.6 两张表）** 会让读者写出**编译不过的代码**，是唯一一类会直接造成实际损失的问题。
- **A1（RectMask2D）** 涉及六章、四种互斥说法，而"Mask 还是 RectMask2D"是本书最常被查询的决策点之一。不统一，这部分内容等于不可用。
- **E1 / E2 / E3** 修的是"读者凭什么相信这本书"。E2 一份文件就可能推翻读者对全书的信任判断，修复成本却极低（加个过期标注即可）。

建议施工顺序：E2 → A1 → A2/B2 → B1 → 其余 A/B → E1/E3/E4/E5。

### 第二批 P1

**C + D**，共 14 条。C1（补 `#include`）是一次性批量修改，成本极低；C2 与 A1 相关，建议随 A1 一起做。D 类按已定方针统一改为定性描述。

### 第三批 P2

**F + G**，共 11 条。F1~F4 是几分钟的改动，可随手做掉；F5~F7 属体例统一，建议作为**独立提交**（`AGENTS.md` 要求全库性改动与内容修改分离）。G 类是选做。

---

## 附：本次已回查并确认**正确**的表述

避免后续维护者重复核查，记录本次实测通过的项：

| 位置 | 表述 | 核查结果 |
|---|---|---|
| 02.7.2 / 05.5.3 | `private static readonly VertexHelper s_VertexHelper = new VertexHelper(Allocator.Domain);` | ✅ 与 main 完全一致 |
| 05.4.2 | `SetVerticesDirty()` 方法体 | ✅ 与 main **逐行一致** |
| 05.4.4 | `SetLayoutDirty()` 方法体 | ✅ 与 main **逐行一致** |
| 01.8 / 导读 | main 目录结构（`com.unity.ugui/Runtime/UGUI/`、`VertexHelper.cs` 在 `Utility/`、`EventSystem/` 与 `UI/Core/` 同级） | ✅ 一致 |
| 15.2.2 | `RectMask2D` 通过 `IClippable.SetClipRect()` / `Cull()` 与子 Graphic 通信，不直接操作 CanvasRenderer | ✅ 与 main 一致（`SetClipRect` / `Cull` / `SetClipSoftness`） |
| 05.5.2 / 08.3.1 | `Graphic.Rebuild()` 只在 `PreRender` 阶段处理、先 Geometry 后 Material | ✅ 与 main 一致 |
| 05.5.4 / 08.3.2 | `materialCount = 1` + `SetMaterial(materialForRendering, 0)` | ✅ 与 main 一致 |
| 08.3.3 | 惰性 `canvasRenderer` 属性 | ✅ 与 main 一致（但同章 8.3.4 / 8.7 未同步，见 A9） |
| 18.1 | `Blend SrcAlpha OneMinusSrcAlpha`、`Cull Off`、`ZWrite Off`、`ZTest [unity_GUIZTestMode]`、Stencil 块由 Material 属性驱动 | ✅ 与 `UI-Default.shader` 一致（但代码块不完整，见 C2） |
