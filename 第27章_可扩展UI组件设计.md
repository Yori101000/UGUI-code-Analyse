# 第27章 可扩展 UI 组件设计

> ⚠️ **先泼盆冷水**：本章讲的"原则"在实际项目中往往是做不到的，也不应该全部照搬。大部分商业项目的 UI 组件都是混合了业务逻辑的——一个背包格子里既显示图标又判断显示品质框又算是否新获得的逻辑。这很正常。**原则是用来参考的，不是用来遵守的。** 能意识到"这里混了逻辑，以后改了这里会受影响"比"我必须把它拆干净"更重要。

---

## 概述

前几章解决了 UGUI 的渲染机制和代码组织，这章讲的是**组件怎么设计才能少踩坑**。不是什么高大上的架构理论，就是一些实践中容易碰到的取舍点：

```
① 到底要不要拆？—— 部件拆分的度
② 复用 vs 硬写？—— 什么值得抽成通用组件
③ UI 之后是什么？—— UGUI 和 UI Toolkit 的现状
```

---

## 27.1 组件的"度"：拆还是不拆

### 27.1.1 拆分的收益

一个纯粹的组件（只做显示）确实好复用：

```csharp
public class ProgressBar : MonoBehaviour
{
    public Image fill;
    public void SetValue(float v) => fill.fillAmount = v;
}
```

同样的 ProgressBar 可以用在血条、加载条、经验条里，不需要改代码。

### 27.1.2 不拆的现实

但实际项目里不可能全都这么干净。一个典型的背包格子：

```csharp
public class BagSlot : MonoBehaviour
{
    public Image icon;
    public Text count;
    public GameObject newBadge;    // "新获得"标记
    public Image qualityBorder;    // 品质边框

    public void Refresh(ItemData data)
    {
        icon.sprite = data.icon;
        count.text = data.count.ToString();
        newBadge.SetActive(data.isNew);
        qualityBorder.color = GetQualityColor(data.quality);
    }

    Color GetQualityColor(Quality q) => q switch
    {
        Quality.White => Color.white,
        Quality.Blue  => Color.blue,
        Quality.Purple => Color.magenta,
        _ => Color.white
    };
}
```

这个组件里混了"显示"和"品质判断逻辑"。按"官方原则"应该把 `GetQualityColor` 抽出去。但在实际项目里，这种做法完全可以接受——只要这个逻辑稳定不变，写在组件里反而比抽成单独的工具类更容易理解。

**关键判断标准不是"有没有混合逻辑"，而是"它会不会变"。**

| 情况 | 建议 |
|------|------|
| 这个逻辑确定不变（如品质颜色映射） | 混在组件里没问题 |
| 这个逻辑可能频繁改（如付费策略、活动规则） | 拆出去，别和 UI 绑死 |
| 这个逻辑有其他模块也在用 | 抽成公共方法 |

---

## 27.2 复用：什么值得抽

**不需要过度抽象。** 只抽两种：**

### 27.2.1 高频出现的 UI 单元

进度条、Toggle、Tab 切换、数字滚动——这些模块里会出现几十次，值得做通用组件。

### 27.2.2 列表 Item

列表 Item 是复用收益最高的场景——配合对象池，几千条数据只创建十几个节点：

```csharp
public class ScrollItemPool
{
    private Queue<ItemView> pool = new Queue<ItemView>();
    private ItemView prefab;

    public ItemView Get()
    {
        if (pool.Count > 0)
        {
            var item = pool.Dequeue();
            item.gameObject.SetActive(true);
            return item;
        }
        return Instantiate(prefab);
    }

    public void Release(ItemView item)
    {
        item.gameObject.SetActive(false);
        pool.Enqueue(item);
    }
}
```

**不需要抽通用组件的**：
- 一个界面只用一次的特殊布局（如主界面的某个活动入口）
- 逻辑高度耦合、拆了反而更难读的小部件

---

## 27.3 一个实际可用的检查清单

与其记一堆原则，不如写代码时过一下这 5 个问题：

```
□ 这块逻辑其他模块也在用吗？
  是 → 抽成工具方法 / 通用组件
  否 → 写在组件里也不怕

□ 这块逻辑后面大概率会改吗？
  是 → 和 UI 分离（不要绑在 MonoBehaviour 里）
  否 → 混着也行

□ 这个组件除了当前界面，其他地方会用到吗？
  是 → 做成通用 Prefab + 数据驱动接口
  否 → 就在当前模块里用，不用抽象

□ 这个组件的 public 方法有没有暴露不该暴露的内部状态？
  有 → 收一下访问权限
  没有 → 跳过

□ 如果以后换 UI 框架（UGUI → UI Toolkit），这部分要重写多少？
  大部分都要重写 → 这是正常的，不用焦虑
```

回答完这几个问题，应该拆还是应该混就清楚了。

---

## 27.4 UI 技术演进

### IMGUI → UGUI → UI Toolkit

| | IMGUI | UGUI | UI Toolkit |
|------|------|------|------|
| 方式 | 每帧 OnGUI 画 | 组件 + Canvas | UXML + USS |
| 渲染 | 即时 | 批处理 | 差量更新 |
| 状态 | 无状态 | 组件持有 | 数据绑定 |
| 用途 | 编辑器工具 | **游戏运行时** | 编辑器 + 逐步覆盖运行时 |

**当前现实**：
- **UGUI 仍占主流**——生态成熟、资源多、与 3D 场景集成方便、团队熟悉。不会很快被替代
- **UI Toolkit** 在编辑器工具领域已经很好用了。运行时还在发展中，新项目可以考虑，迁移老项目成本不低
- **对于大部分游戏项目来说，选 UGUI 在当前时间点仍然是正确选择**

---

## 本章总结

```
不扯原则，就三句话：

① 组件里混了业务逻辑没关系——只要不是高频变化的部分，混着比硬拆好
② 值得抽成通用组件的只有两种：全项目高频出现的 + 列表 Item
③ 过一下 5 问检查清单，比硬套设计模式实用

UI 技术选型：
  UGUI 当前仍是游戏 UI 的主力方案
  UI Toolkit 是下一代方向，但目前运行时生态还在补
```
