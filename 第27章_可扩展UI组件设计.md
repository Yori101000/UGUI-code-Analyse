# 第27章 可扩展 UI 组件设计

> 本章与前面渲染机制章节不同，讲的是 UI 代码层面的**组件设计方法论**——如何设计高复用、低耦合、易维护的 UI 组件。这些原则不局限于 UGUI，在任何 UI 框架下都适用。

---

## 概述

前几章解决了"UGUI 怎么工作"和"代码怎么组织"的问题，这章解决的是"组件怎么写才能复用"。核心关注点：

```
① 设计原则 — 组件职责怎么划分？
② 复用策略 — 同一套 UI 怎么在不同模块复用？
③ 可维护性 — 组件怎么改才不容易出 Bug？
④ UI 技术演进 — UGUI 之后是什么？
```

---

## 27.1 设计原则

### 27.1.1 单一职责

一个组件只做一件事：

```csharp
// ✅ 正确：ProgressBar 只负责进度显示
public class ProgressBar : MonoBehaviour
{
    public Image fill;
    public void SetValue(float v) => fill.fillAmount = v;
}

// ❌ 反例：一个组件同时做显示 + 业务逻辑 + 网络请求
public class InventoryItem : MonoBehaviour
{
    public void SetData(ItemData data)
    {
        icon.sprite = data.icon;
        nameText.text = data.name;
        if (data.isRare) { /* 特殊逻辑 */ }  // 不该在这
    }
}
```

### 27.1.2 数据驱动

UI 只负责展示数据，不处理数据来源：

```csharp
// ✅ 正确：UI 只接收数据
public void SetData(ItemData data)
{
    nameText.text = data.name;
    icon.sprite = data.icon;
}

// ❌ 反例：UI 直接调数据库
public void Awake()
{
    var data = Database.GetItem(123);  // UI 不该做这个
}
```

### 27.1.3 面向接口编程

组件之间通过接口交互，而非具体实现：

```csharp
public interface IItemDataProvider
{
    string GetName();
    Sprite GetIcon();
}

public class ItemView : MonoBehaviour
{
    public void Bind(IItemDataProvider data)
    {
        icon.sprite = data.GetIcon();
        nameText.text = data.GetName();
    }
}
```

任意实现了 `IItemDataProvider` 的对象都可以作为数据源——背包数据、商城数据、任务奖励数据，全部复用同一个 `ItemView`。

### 27.1.4 可组合性

复杂 UI 由多个小组件组合，而非一个大组件包揽所有：

```
ItemView
 ├── IconComponent    ← 独立组件
 ├── NameComponent    ← 独立组件
 ├── QualityBorder    ← 独立组件
 └── CountBadge       ← 独立组件
```

优势：每个子组件独立复用。`IconComponent` 可以在 Item、头像、技能图标中复用，不需要每次重写。

---

## 27.2 复用策略

### 27.2.1 通用组件 vs 业务组件

| 类型 | 特点 | 示例 |
|------|------|------|
| **通用组件** | 与业务无关，全项目复用 | Button、ProgressBar、Toggle、ScrollView Item |
| **业务组件** | 绑定特定业务，复用范围有限 | 背包物品格、技能图标、任务条目 |

原则：优先抽象通用组件，再组合成业务组件——不要直接为某个功能写死一个超大组件。

### 27.2.2 配置驱动

同一套组件通过配置参数产生不同表现：

```csharp
[System.Serializable]
public class ItemStyle
{
    public Color color;
    public Sprite bgSprite;
}

public void ApplyStyle(ItemStyle style)
{
    bgImage.sprite = style.bgSprite;
    nameText.color = style.color;
}
```

应用：品质颜色区分（白/蓝/紫/橙）、不同主题皮肤切换。

### 27.2.3 对象池复用

列表项的复用——这是 UI 组件复用最典型的高频场景：

```csharp
public class ItemPool
{
    private Queue<ItemView> pool = new Queue<ItemView>();
    private ItemView prefab;

    public ItemView Get()
    {
        if (pool.Count > 0)
        {
            var item = pool.Dequeue();
            item.gameObject.SetActive(true);
            return item;
        }
        return Instantiate(prefab);
    }

    public void Release(ItemView item)
    {
        item.gameObject.SetActive(false);
        pool.Enqueue(item);
    }
}
```

配合虚拟列表（见第 19 章 19.6.4），即使数据量上万，场景中只存在十几个 Item 节点。

---

## 27.3 可维护性

### 27.3.1 统一接口规范

所有 UI 组件遵循相同的生命周期：

```csharp
public interface IUIComponent
{
    void OnCreate();           // 实例化后
    void OnBind(object data);  // 绑定数据
    void OnClear();            // 清空状态（回收入池前）
}
```

### 27.3.2 避免硬编码

```csharp
// ❌ 硬编码
text.text = "VIP玩家";
image.color = Color.red;

// ✅ 配置化
text.text = config.vipName;
image.color = config.vipColor;
```

### 27.3.3 组件职责检查清单

```
□ 这个组件是否只做了一件事？
□ 修改它的数据来源是否需要改组件代码？
□ 如果换个项目用，要改多少行？
□ 组件内有没有直接调用全局单例？
□ 组件的 public 方法是否都能被外部随意调用破坏状态？
```

---

## 27.4 UI 技术演进

### 27.4.1 三个阶段

```
IMGUI（即时模式）  →   UGUI（组件模式）     →   UI Toolkit（声明式）
每帧绘制            预制体 + 画布             UXML + USS
工具专属            运行时主流                新一代方案
```

**IMGUI**：`OnGUI()` 每帧画，适合编辑器工具，不适合游戏 UI。

**UGUI**：当前主力。基于 GameObject、Canvas 批处理、可视化编辑。灵活但架构需要自己搭。

**UI Toolkit**：Unity 新一代 UI 系统。使用 UXML（结构） + USS（样式），类似 HTML+CSS。优势是声明式布局、虚拟化列表、差量更新（不触发全量 Rebuild）。目前运行时生态尚未完全成熟。

### 27.4.2 UGUI 的现实定位

尽管 UI Toolkit 在发展，UGUI 在可预见的未来仍将是游戏运行时 UI 的主力方案——原因：生态成熟、与 3D 场景深度结合、灵活性高。UI Toolkit 目前更适合编辑器工具和数据密集型界面。

---

## 本章总结

```
组件设计的四件事：

① 职责单一
   一个组件只做一件事
   数据驱动，UI 不接触业务逻辑

② 复用优先
   通用组件抽象出来
   配置驱动差异化
   对象池复用高频 Item

③ 维护友好
   统一接口规范
   避免硬编码
   检查清单控制复杂度

④ 理解趋势
   UGUI 仍是主力
   UI Toolkit 是下一代方向
   核心能力：设计可扩展的 UI 系统，而不是绑定某一种技术
```
