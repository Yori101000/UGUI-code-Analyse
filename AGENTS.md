# AGENTS.md：本仓库的 Agent 行为守则

> 本文件对所有进入本仓库的 Agent（以及人类协作者）生效。进入仓库后请先阅读，再操作。

## 项目入口

- 仓库入口导航文档：`agent.md`（主题地图、内部编号状态、已知不一致、操作指引）。
- 任务涉及章节内容前，**先读 `agent.md`**；引用章节一律以文件名编号（`第NN章_主题.md`，四部分内 01~25）为准。

## 源码参考规则（强制）

1. **本项目所有内容的判定以 UGUI 官方源码仓库为准**：https://github.com/Unity-Technologies/uGUI/tree/main
2. 当对任何 UGUI 机制、类、方法、文件路径或行为**不确定对错时**，必须先去该仓库核实，再下结论或修改内容；不得凭记忆、二手资料或推测作答。
3. 例外：`Canvas`、`CanvasRenderer`、`RectTransform` 等引擎内置组件不在 uGUI 仓库内（见 `agent.md` 第 3 节"源码引用基线"），此类以 Unity 官方文档与引擎实际行为为准。

## 强制维护义务（保证 agent.md 时效性）

1. **任何内容修改完成后，必须重新审视并同步 `agent.md`。**
   - 修改范围包括但不限于：新增 / 删除 / 重命名章节、调整章节编号、编辑章节正文、更新 `_阅读指南/`、修复勘误、改动文件结构。
   - 需要同步的字段：章节地图（第 2 节）、快速定位表、已知不一致清单（第 4 节），以及文档中任何引用文件名的表述。
2. **修改 `agent.md` 本身时**，同样要保证它与仓库现状一致（例如修正编号错位、替换 X/Y 占位符后立即更新本文件）。
3. 每次修改完成后，在最终回复中说明"已同步 agent.md"或"本次修改不影响 agent.md 内容"。

## 基础约定（细节见 agent.md）

- 全库文件为 UTF-8 无 BOM；PowerShell 读取须加 `-Encoding UTF8`，否则中文乱码。
- 文件名编号（01~25）是当前唯一可信的章节顺序，按目录分四部分：基础理论（01~09）、渲染链路（10~19）、性能与工具（20~21）、工程实践（22~25）。
- 修正源码级错误时优先更新对应「勘误汇总」表（原文声称 / 实际情况），不静默改写正文。
- 大改动（如全库编号统一）应作为独立提交，并与内容修改分离。
